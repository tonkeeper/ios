//
//  {{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}

//  {{ developer.company }}
//  Created by {{ developer.name }} on {{ date }}.
//

import UIKit

class {{ module_info.name }}ViewController: GenericViewController<{{module_info.name}}View> {

  // MARK: - Module

  private let presenter: {{ module_info.name }}PresenterInput

  // MARK: - Init

  init(presenter: {{ module_info.name }}PresenterInput) {
    self.presenter = presenter
    super.init(nibName: nil, bundle: nil)
  }

  required init?(coder: NSCoder) {
    fatalError("init(coder:) has not been implemented")
  }

  // MARK: - View Life cycle

  override func viewDidLoad() {
    super.viewDidLoad()
    setup()
    presenter.viewDidLoad()
  }
}

// MARK: - {{ module_info.name }}ViewInput

extension {{ module_info.name }}ViewController: {{ module_info.name }}ViewInput {}

// MARK: - Private

private extension {{ module_info.name }}ViewController {
  func setup() {}
}
