//
// OpTerminal.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
// Schema version: 2.8.1
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Emitted exactly once per operation when it completes (success, fail, or cancel). Must share operation_id with the corresponding op_attempt. */
public struct OpTerminal: Codable, JSONEncodable, Hashable {

    public enum Flow: String, Codable, CaseIterable {
        case transfer = "transfer"
        case swap = "swap"
        case stake = "stake"
        case tonConnect = "ton_connect"
    }
    public enum Operation: String, Codable, CaseIterable {
        case emulate = "emulate"
        case send = "send"
        case quote = "quote"
        case stake = "stake"
        case unstake = "unstake"
        case connectWallet = "connect_wallet"
        case confirmTransaction = "confirm_transaction"
    }
    public enum Outcome: String, Codable, CaseIterable {
        case success = "success"
        case fail = "fail"
        case cancel = "cancel"
    }
    public var eventName: String = "op_terminal"
    /** Must match the op_attempt event for this operation. */
    public var operationId: String
    /** High-level product flow. */
    public var flow: Flow
    /** Operation within the flow. */
    public var operation: Operation
    /** Terminal outcome of the operation. */
    public var outcome: Outcome
    /** Duration of the operation in milliseconds. */
    public var durationMs: Double
    /** End time in milliseconds since Unix epoch. */
    public var finishedAtMs: Int
    /** Set when outcome is fail; error code if available. */
    public var errorCode: Int?
    /** Set when outcome is fail; human-readable error message. */
    public var errorMessage: String?
    /** Set when outcome is fail; normalized error category for grouping. */
    public var errorType: String?
    /** Set when outcome = {fail,cancel}. Stage where the operation ended. When outcome is fail, stage where failure occurred (e.g. emulate, broadcast). When outcome is cancel, stage where user cancelled.  */
    public var stage: String?
    /** Optional. Additional metadata in JSON format that may be relevant for debugging RED metrics, e.g. network for send operation (ton, trc20, etc), provider name, fee payment method.  */
    public var otherMetadata: String?

    public init(eventName: String = "op_terminal", operationId: String, flow: Flow, operation: Operation, outcome: Outcome, durationMs: Double, finishedAtMs: Int, errorCode: Int? = nil, errorMessage: String? = nil, errorType: String? = nil, stage: String? = nil, otherMetadata: String? = nil) {
        self.eventName = eventName
        self.operationId = operationId
        self.flow = flow
        self.operation = operation
        self.outcome = outcome
        self.durationMs = durationMs
        self.finishedAtMs = finishedAtMs
        self.errorCode = errorCode
        self.errorMessage = errorMessage
        self.errorType = errorType
        self.stage = stage
        self.otherMetadata = otherMetadata
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case eventName
        case operationId = "operation_id"
        case flow
        case operation
        case outcome
        case durationMs = "duration_ms"
        case finishedAtMs = "finished_at_ms"
        case errorCode = "error_code"
        case errorMessage = "error_message"
        case errorType = "error_type"
        case stage
        case otherMetadata = "other_metadata"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(eventName, forKey: .eventName)
        try container.encode(operationId, forKey: .operationId)
        try container.encode(flow, forKey: .flow)
        try container.encode(operation, forKey: .operation)
        try container.encode(outcome, forKey: .outcome)
        try container.encode(durationMs, forKey: .durationMs)
        try container.encode(finishedAtMs, forKey: .finishedAtMs)
        try container.encodeIfPresent(errorCode, forKey: .errorCode)
        try container.encodeIfPresent(errorMessage, forKey: .errorMessage)
        try container.encodeIfPresent(errorType, forKey: .errorType)
        try container.encodeIfPresent(stage, forKey: .stage)
        try container.encodeIfPresent(otherMetadata, forKey: .otherMetadata)
    }
}


