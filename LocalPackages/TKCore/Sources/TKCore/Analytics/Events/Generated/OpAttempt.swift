//
// OpAttempt.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
// Schema version: 2.8.1
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Emitted once per user operation attempt. Use same operation_id in the corresponding op_terminal event. */
public struct OpAttempt: Codable, JSONEncodable, Hashable {

    public enum Flow: String, Codable, CaseIterable {
        case transfer = "transfer"
        case swap = "swap"
        case stake = "stake"
        case tonConnect = "ton_connect"
    }
    public enum Operation: String, Codable, CaseIterable {
        case emulate = "emulate"
        case send = "send"
        case quote = "quote"
        case stake = "stake"
        case unstake = "unstake"
        case connectWallet = "connect_wallet"
        case confirmTransaction = "confirm_transaction"
    }
    public var eventName: String = "op_attempt"
    /** Unique id for this attempt; must match the op_terminal event for the same operation. */
    public var operationId: String
    /** High-level product flow. */
    public var flow: Flow
    /** Operation within the flow. transfer: emulate | send. swap: quote | emulate | send. stake: stake | unstake. ton_connect: connect_wallet | confirm_transaction.  */
    public var operation: Operation
    /** Optional. Where the attempt originated (e.g. native_ui, tonconnect_local, tonconnect_remote). */
    public var attemptSource: String?
    /** Start time of the operation in milliseconds since Unix epoch. */
    public var startedAtMs: Int
    /** Optional. Additional metadata in JSON format that may be relevant for debugging RED metrics, e.g. network for send operation (ton, trc20, etc), provider name, fee payment method.  */
    public var otherMetadata: String?

    public init(eventName: String = "op_attempt", operationId: String, flow: Flow, operation: Operation, attemptSource: String? = nil, startedAtMs: Int, otherMetadata: String? = nil) {
        self.eventName = eventName
        self.operationId = operationId
        self.flow = flow
        self.operation = operation
        self.attemptSource = attemptSource
        self.startedAtMs = startedAtMs
        self.otherMetadata = otherMetadata
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case eventName
        case operationId = "operation_id"
        case flow
        case operation
        case attemptSource = "attempt_source"
        case startedAtMs = "started_at_ms"
        case otherMetadata = "other_metadata"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(eventName, forKey: .eventName)
        try container.encode(operationId, forKey: .operationId)
        try container.encode(flow, forKey: .flow)
        try container.encode(operation, forKey: .operation)
        try container.encodeIfPresent(attemptSource, forKey: .attemptSource)
        try container.encode(startedAtMs, forKey: .startedAtMs)
        try container.encodeIfPresent(otherMetadata, forKey: .otherMetadata)
    }
}


